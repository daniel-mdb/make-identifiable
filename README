It became harder to debug modern c++ code due the proliferation of
anonymous lambdas.

This "demo" injects a function preamble through "make_identifiable"
into any function, which upon a call must print its name, source file
and line.
